const canvas=document.getElementById('matrix');const ctx=canvas.getContext('2d');let w,h,cols,drops;function size(){w=canvas.width=innerWidth;h=canvas.height=innerHeight;cols=Math.floor(w/15);drops=Array(cols).fill(0).map(()=>Math.random()*-80)}function draw(){ctx.fillStyle='rgba(0,0,0,.12)';ctx.fillRect(0,0,w,h);ctx.fillStyle='#00ff87';ctx.font='12px monospace';for(let i=0;i<cols;i++){const c=String.fromCharCode(0x30a0+Math.random()*96);ctx.fillText(c,i*15,drops[i]*15);if(drops[i]*15>h&&Math.random()>.975)drops[i]=0;drops[i]++}requestAnimationFrame(draw)}addEventListener('resize',size);size();draw();
const links=[...document.querySelectorAll('nav a[href^="#"]')];const sections=[...document.querySelectorAll('main section[id]')];const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+e.target.id))}}),{rootMargin:'-30% 0px -55%'});sections.forEach(s=>obs.observe(s));
